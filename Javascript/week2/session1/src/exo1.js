/* EXERCICE 1 - CHANGE LA COULEUR DE FOND DU BODY */

// OBJECTIF:
// Cet exercice a pour but de vous faire utiliser les conditions
// avec if else et else if pour changer la couleur de fond du body


// Cette fonction est executée à chaque fois que l'utilisateur va changer
// la valeur de l'élément "select" dans le HTML, c'est à dire, à chaque fois
// qu'il va "changer" d'option... c'est pourquoi elle est liée à l'événement onChange
// dans le code HTML.
function selectChange() {

  // cette variable récupère la valeur de l'option sélectionnée.
  var color = document.getElementById('colors').value;

  // cette partie du code te permets de voir ce qu'à choisi l'utilisateur
  console.log("l'utilisateur a choisi: ", color);

  // CONSIGNE:
  // Écris le code pour changer la couleur de fond du body en fonction de
  // la valeur qu'a choisi l'utilisateur, en t'aidant des conditions (if)

  // Pour changer la couleur de fond du body, utilise le code ci-dessous:
  // document.body.style.backgroundColor = 'la-couleur-en-css';


  /* MON CODE EN DESSSOUS */

  /* MON CODE AU DESSUS */

};
